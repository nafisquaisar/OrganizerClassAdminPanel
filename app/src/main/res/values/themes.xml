<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.OrganizerAdminPanel" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Set white for icons on primary background -->
        <!-- Primary color customization -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary</item> <!-- For older API versions -->
        <item name="android:statusBarColor">@color/primary</item> <!-- For modern API versions -->
        <item name="android:windowLightStatusBar">false</item> <!-- Ensure icons on the status bar are white -->

    </style>

    <style name="Theme.OrganizerAdminPanel" parent="Base.Theme.OrganizerAdminPanel" />
</resources>
